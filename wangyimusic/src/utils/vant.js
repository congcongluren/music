import Vue from "vue"

import {
  NavBar,
  Tab, Tabs,
  Form, Field,
  Button,
  Icon,
  Search,
  Overlay
} from "vant"

Vue.use(Tab)
   .use(Tabs)
   .use(Form)
   .use(Field)
   .use(Button)
   .use(NavBar)
   .use(Icon)
   .use(Search)
   .use(Overlay)
